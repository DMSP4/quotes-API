
const config = {
  PUBLIC_API_URL: "https://quoteslate.vercel.app/api/quotes/random",
  API_URL: "http://localhost:3000",
};

export default config;
// This file contains the configuration for the API URLs used in the application.
